/* ============================================================
   POOJA'S COUTURE — Supabase Configuration
   ------------------------------------------------------------
   FILL IN YOUR PUBLISHABLE KEY BELOW.
   - The URL is safe to keep here (it's not secret).
   - The publishable (anon) key is safe in frontend code ONLY
     once Row Level Security is enabled (a later step).
   - NEVER put your SECRET key in this file or anywhere in the app.
   ============================================================ */

const SUPABASE_CONFIG = {
  url: 'https://sgzbuipldbjfbhtatwjc.supabase.co',
  anonKey: 'PASTE_YOUR_PUBLISHABLE_KEY_HERE'   // <-- replace this string
};
